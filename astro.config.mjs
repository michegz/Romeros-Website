import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://romerosservicecompany.com',
  integrations: [],
  output: 'static',
  trailingSlash: 'never',
});
// @astrojs/sitemap will be re-added once Phase 1 pages are built
